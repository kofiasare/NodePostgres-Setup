const apiController = require('../controllers')

// sub-routers
const v1 = require('./v1')

module.exports = (express) => {}

module.exports = {

    v1
}

let info = {
    version: 1,
    docURL: 'https://postman.com/...'
}